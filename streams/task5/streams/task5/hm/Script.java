package streams.task5.hm;
import oop.streams.InputStream;
import oop.streams.OutputStream;

public class Script {
  /*
   * words: the array of secret words.
   * is: the input stream that provides the player's moves.
   * os: the output stream to print to.
   */
  public Script(String words[], InputStream is, OutputStream os) {
	  
  }

  /*
   * Plays the script.
   */
  public void play() {
	  
  }

  /*
   * Give access to the instance of our game.
   */
  public HangedMan game() {
	  
  }
}